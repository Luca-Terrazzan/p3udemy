# tuples examples
# similar to lists, but immutable

my_tuple = (1, 2, 2, 2, 2, 2, 3)
my_list = [1, 2, 3]

# two methods: count() and index()
print(my_tuple.count(2))
print(my_tuple.index(3))
